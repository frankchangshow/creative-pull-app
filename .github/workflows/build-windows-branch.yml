name: Build Windows App (Branch)

on:
  push:
    branches: ['**']
  workflow_dispatch: {}

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build with PyInstaller
      shell: pwsh
      run: |
        pyinstaller --noconfirm --onedir --windowed --name "CreativePullApp" `
          --hidden-import tkinter `
          --hidden-import "ui.advanced" `
          --hidden-import "ui.preview" `
          --hidden-import "ui.settings" `
          --hidden-import "utils.har_utils" `
          --hidden-import "utils.markup_parser" `
          --hidden-import "utils.vast_utils" `
          --hidden-import "clients.databricks_client" `
          --hidden-import "clients.savanna_client" `
          --hidden-import "services.savanna_actions" `
          --hidden-import "services.watchlist" `
          --hidden-import "config.app_config" `
          --hidden-import "savanna_bearer_client" `
          --add-data "databricks_job_templates;databricks_job_templates" `
          creative_previewer_app_webview.py

    - id: sanitize
      name: Sanitize ref name
      shell: pwsh
      run: |
        $branch='${{ github.ref_name }}'
        $safe=$branch -replace '[\\/:\*\?"<>\| ]','-'
        echo "SAFE_REF=$safe" >> $env:GITHUB_OUTPUT

    - name: Package application
      shell: pwsh
      run: |
        $safe='${{ steps.sanitize.outputs.SAFE_REF }}'
        Compress-Archive -Path dist/CreativePullApp -DestinationPath ("CreativePullApp_Windows_{0}.zip" -f $safe)

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: CreativePullApp_Windows_${{ steps.sanitize.outputs.SAFE_REF }}
        path: CreativePullApp_Windows_${{ steps.sanitize.outputs.SAFE_REF }}.zip
        if-no-files-found: error
